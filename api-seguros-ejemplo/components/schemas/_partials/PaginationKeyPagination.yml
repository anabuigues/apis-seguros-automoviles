type: object
additionalProperties: false
required:
  - pagination_key
properties:
  pagination_key:
    type: string
    description: |
      Siguiente valor a enviar en el query parameter pagination_key para obtener la siguiente página
      de resultados. Este valor no cambiará una vez devuleta la útlima página de resulados.
    example: 'ABCD0324324'
discriminator:
  propertyName: todo